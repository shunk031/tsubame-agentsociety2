# AGENTS.md

このリポジトリ固有の取り決め。手順は `README.md` にあるので繰り返さない。

## 知見をどこに書くか

文書を 2 つに分ける。

| 文書        | 中身                     | git    |
| ----------- | ------------------------ | ------ |
| `README.md` | 手順と、再現済みの知見   | 追跡    |
| `NOTES.md`  | 運用メモと積み残し・事情 | 管理外 |

実機で再現・確認できた挙動は追跡対象の文書に書く。症状・原因・対処を 1 項目にまとめ、
中身で行き先を決める。**`NOTES.md` だけに書いて終わりにしない**。
`NOTES.md` は git 管理外なので、新しいクローン・別のマシン・別のエージェントには届かない。

| 挙動の性質                                     | 行き先                             |
| ---------------------------------------------- | ---------------------------------- |
| この環境・スケジューラ・配信の罠               | `README.md` の「落とし穴」         |
| agentsociety2 自体の挙動（どこで動かしても同じ）| `docs/agentsociety2-behaviour.md`  |

README は手順書なので、その手順をたどる途中で踏むものだけを置く。
ライブラリの意味論はシナリオを設計するときに読むもので、読む人もタイミングも違う。

`NOTES.md` に残すのは、そのマシンやその時点にしか当てはまらないものに限る。
手元の環境固有の不具合、走っているジョブの ID、次に何をするかの覚書がこれにあたる。

「落とし穴」に並べるのは実機で再現したものだけ。調べただけで確かめていないことは、
そう分かる書き方にする。

## worktree を作ったとき

`config/env.local` は git 管理外なので `git worktree add` は複製しない。先に `cp` する。

```bash
git worktree add <path> -b <branch> main
cp config/env.local <path>/config/env.local
```

無いまま `scripts/lib/check_common.sh` を走らせると、`common.sh` が `TSUBAME_GROUP` 未設定で
落ちて出力が空になり、値の存在を見るアサーションだけがまとめて失敗する。
スクリプトの不具合に見えるが、原因は設定ファイルの不在。
